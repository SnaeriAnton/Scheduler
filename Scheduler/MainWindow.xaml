<Window x:Class="Scheduler.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        
        xmlns:local="clr-namespace:Scheduler"
        mc:Ignorable="d"
        Title="Scheduler" Width="1200" Height="800" Loaded="WindowLoaded" Closed="WindowClosed">
    <Grid Background="#ffdadada">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="556*"/>
            <ColumnDefinition Width="41*"/>
            <ColumnDefinition Width="596*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition Height="60" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Margin="3" Background="White" Padding="30" CornerRadius="50" Grid.ColumnSpan="3">
            <TextBlock x:Name="timerView" Foreground="red" Text="00:00:00" FontSize="260" FontWeight="Bold" Margin="0" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>

        <Border Grid.Row="1" Margin="3" Background="White" Padding="30" CornerRadius="5" Grid.ColumnSpan="3">
        </Border>

        <Border Grid.Row="2" Margin="3" Background="White" Padding="30" CornerRadius="5" Grid.ColumnSpan="3">
        </Border>

        <DataGrid x:Name="dgTaskList" Grid.Row="1" FontSize="17" FontWeight="Bold" Grid.ColumnSpan="3" AutoGenerateColumns="False" SelectedCellsChanged="DgTodoListSelectedCellsChanged" >
            <DataGrid.Columns>
                <DataGridTextColumn x:Name="date" Binding="{Binding Path = CreationDate}" IsReadOnly="true" Header="Дата создания" Width="200"/>
                <DataGridCheckBoxColumn x:Name="status" Binding="{Binding Path = Status, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="false" Header="Статус" Width="90"/>
                <DataGridTextColumn x:Name="timer" Binding="{Binding Path = Time}" IsReadOnly="false" Header="Таймер" Width="105"/>
                <DataGridTextColumn x:Name="task" Binding="{Binding Path = Task}" IsReadOnly="false" Header="Задача" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>


        <Button x:Name="buttonDelit" Grid.Column="0" Grid.Row="2" Width="100" Height="50" Click="ButtonPlayClick" Margin="248,5,207.5,5">
            <materialDesign:PackIcon x:Name="iconButtonPlay" Kind="Play" Height="40" Width="40"/>
        </Button>

        <Button x:Name="buttonDelete" Grid.Column="2" Grid.Row="2" Width="100" Height="50" Click="ButtonDeleteClick" Margin="248,5">
            <materialDesign:PackIcon Kind="Delete" Height="40" Width="40"/>
        </Button>


    </Grid>
</Window>
